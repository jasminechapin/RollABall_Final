using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Powerup : MonoBehaviour
{
    string powerup_type;

    // Start is called before the first frame update
    void Start()
    {
        powerup_type = gameObject.tag;
    }

    


    // Update is called once per frame
    void Update()
    {
        
    }
}
